name: 部署Node.js后端代码到服务器

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 调试-列出仓库文件
      run: |
        echo "当前工作目录: $GITHUB_WORKSPACE"
        ls -la $GITHUB_WORKSPACE
        find $GITHUB_WORKSPACE -type f ! -name '.*'

    - name: 配置SSH环境
      run: |
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' > ~/.ssh/github_actions_key
        chmod 600 ~/.ssh/github_actions_key
        ssh-keyscan -p ${{ secrets.SSH_PORT }} ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

    - name: 安装Rsync
      run: sudo apt-get update && sudo apt-get install -y rsync

    - name: 同步文件到服务器
      run: |
        rsync -avzvv --progress --delete \
          --include='*' \                   # 强制包含所有文件
          --exclude='node_modules' \        # 再排除特定目录
          --exclude='.git' \
          --copy-links \
          -e "ssh -vvv -i ~/.ssh/github_actions_key -o StrictHostKeyChecking=no -p ${{ secrets.SSH_PORT }}" \
          $GITHUB_WORKSPACE/ \
          ${{ secrets.SSH_USERNAME }}@${{ secrets.SERVER_IP }}:/www/wwwroot/egg-blog/ \
          > rsync-debug.log 2>&1
        cat rsync-debug.log

    - name: 修复服务器权限
      run: |
        ssh -i ~/.ssh/github_actions_key -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USERNAME }}@${{ secrets.SERVER_IP }} \
          "chmod -R 755 /www/wwwroot/egg-blog/ && chown -R www-data:www-data /www/wwwroot/egg-blog/"
